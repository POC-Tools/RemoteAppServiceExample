<Page
    x:Class="AppServiceProvider.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppServiceProvider"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Info:" FontSize="24" Margin="10" />
            <TextBox Text="{x:Bind FamilyName}" IsReadOnly="True" Margin="10" FontSize="24" TextWrapping="Wrap" />
            <TextBox Text="{x:Bind IpAddress}" IsReadOnly="True" Margin="10" FontSize="24" TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</Page>
